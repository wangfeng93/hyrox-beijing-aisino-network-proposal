# 主动风险检查模块验收记录

## 首轮浏览器检查

| 检查项 | 结果 |
|---|---|
| 主页面新增导航 | “主动检查”入口可见并正确定位到新模块 |
| 北京事实修正 | 页面明确写明北京站未安排直播、采用专线技术且未发生故障 |
| 上海规划假设 | 页面明确写明上海站先按有直播设计和规划 |
| 演示核心逻辑 | 完整流程单步至 T+019s 后，主路渐变退化触发备用 A 接管，累计切换 1 次 |
| 流程留痕 | 测试时已生成 9 条模拟事件记录 |
| 内嵌模式 | 本地静态服务器重定向移除了查询参数，导致独立页头部和说明区仍显示在 iframe 中；需改为哈希参数后复测 |

该演示为纯前端内存模拟，不访问真实德国服务器、不扫描端口、不调用设备接口，也不修改任何网络配置。

## 修正后浏览器复测

| 检查项 | 结果 |
|---|---|
| 内嵌模式 | iframe 正确进入 `embed` 模式，独立页顶部与介绍区隐藏，控制台直接呈现 |
| iframe 自适应高度 | 桌面端计算高度为 1,332 px，主页面无横向溢出 |
| 完整流程 | T+036s 时由主路 P 切换至备用 A，共切换 1 次；主路恢复后保持备用承载，显示“默认不抢占” |
| 共同异常 | 三路业务同时异常时保持主路 P、0 次切换，判定为“疑似服务端 / 共享依赖异常” |
| 容量不足 | 备用 A/B 超过准入负载时保持主路 P、0 次切换，判定为“没有合格备路：告警，不盲切” |

上述结果验证了连续确认、备用准入、防抖、切换后复核以及共同异常冻结等核心设计逻辑。

## 响应式视觉检查

浏览器沙盒在 `#active-risk` 位置的实际视图显示：新增章节标题、五项机制卡、风险边界说明和嵌入控制台层级清晰，控制台已去除重复页头。主页面在 1,280 px 浏览器视口下无横向溢出。

Chromium 命令行直接访问带锚点地址时没有稳定停留在目标章节，因此生成的两张命令行截图仅用于确认原首屏桌面与移动布局未受本次修改破坏，不作为新增章节定位证据；新增章节视觉以浏览器沙盒的实际锚点视图与 DOM 检查为准。

## 独立页面与控制台检查

主页面及 iframe 控制台未出现新的脚本或资源错误。独立演示页完整保留合作标识、导航、方案介绍、八类故障注入场景、三路径指标卡、策略参数、趋势图、事件日志、方案逻辑、落地边界与技术参考。桌面首屏中三路拓扑、状态门禁和参数控制信息可读，交互控件均可访问。

## 八场景策略验收

| 场景 | 12 轮后的结果 |
|---|---|
| 正常运行 | 保持主路 P，0 次切换 |
| 主路渐变劣化 | 切换至备用 A，1 次切换 |
| 主路突发断线 | 切换至备用 A，1 次切换 |
| 主路与备用 A 同时失效 | 仅备用 B 合格，切换至 B，1 次切换 |
| 服务器 / 共享依赖异常 | 保持主路 P，0 次切换，冻结盲目换路 |
| 短暂抖动 | 保持主路 P，0 次切换，连续异常计数未满足 |
| 备路容量不足 | 保持主路 P，0 次切换，拒绝不合格备路 |
| 探针观测失联 | 保持主路 P，0 次切换，状态标记未知并告警 |

测试后浏览器控制台无脚本或资源错误。
